# JiraTesting Node.js Project

This is a Node.js project for creating scripts to interact with Jira.

## How to Run

1. Make sure you have Node.js installed.
2. Open a terminal in this directory.
3. Run `npm install` to install any dependencies you add to `package.json`.
4. Run the following command to execute the main script:

    ```bash
    npm start
    ```